import { TitleGeneric } from '../../../components'
import type { {{pascalCase name}}Props } from '../types'

export function {{pascalCase name}}View({ title }: {{pascalCase name}}Props) {
  return <TitleGeneric title={title} />
}
